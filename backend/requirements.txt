fastapi==0.115.0
uvicorn[standard]==0.32.0
sqlalchemy==2.0.35
asyncpg==0.29.0
pgvector==0.3.5
redis==5.1.1
python-dotenv==1.0.1
pydantic-settings==2.14.2
langchain==1.3.14
langchain-openai==1.4.0
langgraph==1.2.9
openai==2.47.0
groq==0.37.1
langchain-groq==1.1.3
boto3==1.35.36
python-multipart==0.0.12
alembic==1.13.3
pymupdf==1.28.0
python-docx==1.2.0
rank-bm25==0.2.2
sentence-transformers==5.6.0
torch==2.13.0
webrtcvad-wheels==2.0.14
reportlab==4.2.5
matplotlib==3.9.2
sendgrid==6.11.0
email-validator==2.3.0
ragas==0.4.3
mlflow==3.14.0
prometheus-client==0.25.0
# NOTE: evidently is deliberately NOT used for drift detection (Module 7) — evidently
# 0.7.21 unconditionally imports its litestar-based UI server, which depends on a
# `multipart` package that occupies the same import namespace as `python-multipart`
# (FastAPI/Starlette's actual multipart dependency) and silently overwrites it.
# Verified via a real upload request that this broke file uploads. PSI-based drift
# detection is implemented directly instead (see app/services/mlops/drift_detector.py).
python-jose[cryptography]==3.5.0
bcrypt==5.0.0
httpx==0.28.1
# NOTE: aioredis is NOT used for the notification queue (Module 8) — aioredis is
# deprecated/unmaintained; its asyncio support was merged into redis-py itself years
# ago. This app already uses `redis.asyncio` everywhere (see core/redis_client.py),
# which is the modern equivalent the aioredis project itself recommends migrating to.
apscheduler==3.11.3
slowapi==0.1.10
